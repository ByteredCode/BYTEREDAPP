[tool.pytest.ini_options]
asyncio_mode = "auto"
testpaths = ["tests"]
pythonpath = ["."]

[tool.coverage.run]
source = ["app"]
omit = ["app/core/database.py", "tests/*"]

[tool.coverage.report]
fail_under = 80
